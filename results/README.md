# Generated results

This directory is for reproducible simulation outputs, local logs, CSV/ROOT
files, and generated plots. Its contents are ignored by Git except for this
README and ".gitkeep".

If a representative figure is needed as a permanent project artifact, copy
the reviewed final version into "../docs/figures/" and record its generating
command, input commit, macro, random seed, and event count in the validation
log.

